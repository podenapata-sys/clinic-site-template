{
  "name": "clinic-site-template",
  "version": "1.0.0",
  "private": true,
  "description": "White-label clinic website template — bilingual, WhatsApp booking, pricing calculator, Firebase-backed dashboard. Zero build step.",
  "type": "module",
  "scripts": {
    "check":  "node scripts/apply-config.mjs --check",
    "apply":  "node scripts/apply-config.mjs",
    "diff":   "node scripts/apply-config.mjs --dry-run",
    "rename": "node scripts/rename-clinic.mjs",
    "dev":    "python3 -m http.server 8000"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
